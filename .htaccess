# This file lives ABOVE public_html on Hostinger.
# It protects the app/, config/, database/, storage/ directories.

# Deny ALL web access to everything above public_html
Options -Indexes

<FilesMatch ".*">
    Order allow,deny
    Deny from all
</FilesMatch>
